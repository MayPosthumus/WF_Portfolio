import { Outlet, createBrowserRouter, RouterProvider, Route, NavLink } from "react-router-dom";

const Portfolio = () => {
    return (
        <div>
            <a href="/slotmachine" >SlotMachine</a>
        </div>
    );
}

export default Portfolio;